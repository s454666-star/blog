{
  "name": "frame-video-journal",
  "version": "1.0.0",
  "description": "美少女映像管 — local soft-girl CRT video archive desktop client",
  "private": true,
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "package": "electron-packager . FrameJournal --platform=win32 --arch=x64 --out=dist --asar --overwrite --ignore=\"^/(dist|tests|install.ps1)$\" --icon=assets/icon.ico"
  },
  "devDependencies": {
    "electron": "44.4.5",
    "@electron/packager": "20.3.0"
  }
}
